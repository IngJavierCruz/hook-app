const procesoIterativo = (iteraciones) => {

    for (let index = 0; index < iteraciones; index++) {
        console.log(' Recorriendo');
    }

    return `${ iteraciones } iteraciones realizadas`;
    
}

export default procesoIterativo;